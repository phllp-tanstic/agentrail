import { ORDER_TYPE, SELF_MATCHING_OPTION, ORDER_KIND_SIDE, sideOfKind } from '@somnia-chain/markets-sdk';
console.log('ORDER_TYPE =', JSON.stringify(ORDER_TYPE));
console.log('SELF_MATCHING_OPTION =', JSON.stringify(SELF_MATCHING_OPTION));
console.log('ORDER_KIND_SIDE =', JSON.stringify(ORDER_KIND_SIDE));
